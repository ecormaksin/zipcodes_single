SELECT
	ZC.ID
FROM
	(
	SELECT
		JAPANESE_LOCAL_GOVERMENT_CODE
	,
		MIN(ID) AS MIN_ID
	FROM
		ZIP_CODE.ZIP_CODES
	WHERE
		PREFECTURE_NAME = '東京都'
	GROUP BY
		JAPANESE_LOCAL_GOVERMENT_CODE
) SQ
INNER JOIN
ZIP_CODE.ZIP_CODES ZC
	ON
	ZC.ID = SQ.MIN_ID;